# Express-Angular-CRUDApp
Simple CRUD Application with Express and Angular

> Set up frontend
        
        goto -> frontend
        npm install
        ng serve --open
        
        
> Set up backend
   
        goto -> backend
        npm install
        npm start
        
